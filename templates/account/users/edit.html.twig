{% extends 'account/base_account.html.twig' %}

{% block title %}Edition administrateur{% endblock %}

{% block body %}

    {{ include('account/users/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    <div class="d-flex mt-3">
        <a href="{{ path('app_users_account_index', {'token':app.user.fkCompany.token}) }}" class="btn btn-info mr-3"><i class="fa-solid fa-chevron-left fa-sm mr-3"></i>Retour</a>
        {% if user.id != app.user.id %}
            {{ include('account/users/_delete_form.html.twig') }}
        {% endif %}
    </div>
{% endblock %}
